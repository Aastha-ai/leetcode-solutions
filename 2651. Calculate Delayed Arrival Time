class Solution:
    def findDelayedArrivalTime(self, at: int, dt: int) -> int:
        if (at+dt==24):
            return 0
        elif (at+dt>24):
            return (at+dt)-24
        else:
            return (at+dt)
