class Solution:
    def isPalindrome(self, s: str) -> bool:
        s1=""
        for i in s:
            if i.isalpha() or i.isnumeric():
                s1+=i
        s1=s1.lower()
        print(s1)
        if s1==s1[::-1]:
            return 1
        return 0
