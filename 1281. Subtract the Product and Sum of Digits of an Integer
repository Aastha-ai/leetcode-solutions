class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        l=(int(i)for i in str(n))
        p=1
        s=0
        for i in l:
            p*=i
            s+=i
        return (p-s)
